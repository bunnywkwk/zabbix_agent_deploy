# Old offline-fallback RPM binaries (~13MB, 4 files). No longer used by
# the role (internet install only); safe to delete the files and this rule.
files/*.rpm

# Ansible runtime artifacts
*.retry
*.pyc
__pycache__/

# Editor/OS cruft
.DS_Store
.ansible/
