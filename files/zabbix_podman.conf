# /etc/zabbix/zabbix_agent2.d/zabbix_podman.conf   (mode 0644, owner root:root)
#
# One UserParameter for the whole cluster. Everything the template needs is
# carried in this single JSON document and split apart by dependent items.
UserParameter=podman.status,/etc/zabbix/scripts/podman_status.sh
